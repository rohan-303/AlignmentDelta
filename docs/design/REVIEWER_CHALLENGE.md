# Step 2.1 reviewer challenge

| Dimension | Score (1–10) | Strict assessment |
|---|---:|---|
| Importance | 7 | Separating refusal from collateral behavior matters for safety interpretation. |
| Novelty | 6 | Controls and objective calibration help, but dose-response and safety-removal overlap remain substantial. |
| Causal identification | 8 | Same-checkpoint intervention/control contrasts are stronger than before/after comparisons. |
| Experimental controls | 8 | Orthogonal matched random directions address generic-perturbation criticism. |
| Statistical design | 7 | Repeated-item structure and family confounding are acknowledged; final sample-size power work remains. |
| Compute feasibility | 6 | Primary 3B-class models are cloud-oriented and gated access is a real risk. |
| Reproducibility | 7 | Revisions, manifests, deterministic profiles, and fixed operator rules support replication. |
| Paper potential | 7 | Potentially publishable if the effect/control separation is clear; a null can still be informative. |

**Comparison with Ablating Safety:** probably, but not unconditionally. If executed exactly as designed, the project is differentiated by its primary matched generic-perturbation control, objective calibration, semantic-consistency outcome, and response-curve interaction rather than by safety removal itself. The answer becomes `uncertain` if the benchmark/license gates fail, the random controls are not genuinely matched, or the final analysis collapses to a simple before/after refusal score. The design must not proceed to confirmatory claims until those missing gates are resolved.

Remaining reviewer concern: one checkpoint per family limits family-level inference, and the direction-estimation/site-selection protocol must be frozen and independently validated before data collection.
