# Step 3 engineering scope

After `ENGINEERING_GO`, Step 3 may:

1. download only Qwen2.5-1.5B-Instruct at revision `989aa7980e4cf806f80c7fef2b1adb7bc71aa306`;
2. load it with `trust_remote_code=False`;
3. run benign diagnostic forward-pass checks;
4. capture hidden states;
5. construct a direction artifact from the pinned contrast source;
6. construct deterministic orthogonal random controls;
7. apply the signed projection operator at the specified technical site;
8. run tiny synthetic/technical intervention checks;
9. measure engineering-only memory and runtime diagnostics.

Step 3 must not run full XSTest, HarmBench, or MMLU evaluation; generate scientific response curves; create confirmatory tables; or make paper claims. Step 2.4 does not authorize Step 3 execution automatically; the next prompt must explicitly authorize it.
